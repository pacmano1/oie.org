node_modules/
dist/
.astro/
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
.env.*
!.env.example

# Claude Code local settings (per-developer, not for the repo)
.claude/

# Local deploy configuration (bucket name, distribution ID)
deploy.config.local.sh

# Eclipse Foundation brand assets (reference only, not part of the build)
EF Logo/

# Share card generator working files
scripts/brand/Raleway.ttf
scripts/brand/.tmp/
