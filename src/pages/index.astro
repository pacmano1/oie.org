---
import BaseLayout from '../layouts/BaseLayout.astro';
import Icon from '../components/Icon.astro';
import { getCollection } from 'astro:content';
import { sponsors } from '../data/sponsors';
import { vendors } from '../data/vendors';
import { plugins } from '../data/plugins';

const latestNews = (await getCollection('news'))
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 3)
  .map((p) => ({
    href: `/news/${p.id}/`,
    title: p.data.title,
    excerpt: p.data.excerpt,
    date: p.data.date,
  }));

const newsFmt = new Intl.DateTimeFormat('en-US', { year: 'numeric', month: 'long', day: 'numeric', timeZone: 'UTC' });
---

<BaseLayout
  title="Open Integration Engine - Open Source Healthcare Integration"
  description="Open Integration Engine (OIE) is a free, open source healthcare integration engine. Community-driven, vendor-independent, and fully compatible with Mirth Connect."
>
  <section class="hero">
    <div class="container hero__inner">
      <div class="hero__logo">
        <img src="/images/oie-logo.png" alt="Open Integration Engine logo" width="360" height="360" />
      </div>
      <div class="hero__copy">
        <h1 class="hero__lead">
          <span>Healthcare Integration</span>
          <span>Free and Open Source</span>
          <span>No Vendor Lock-In</span>
        </h1>

        <p class="hero__sub">
          A community-driven open source engine, fully compatible with Mirth<sup>®</sup> Connect and extended by a growing ecosystem of free plugins.
        </p>

        <p class="hero__cta">
          <a class="button" href="/downloads/">Download</a>
        </p>
      </div>
    </div>
  </section>

  <section class="compare-callout">
    <div class="container">
      <div class="card compare-callout__card">
        <div class="compare-callout__copy">
          <h2 class="compare-callout__title">Choosing an integration engine?</h2>
          <p class="compare-callout__text">
            See how OIE compares to BridgeLink and Mirth Connect on licensing, plugins, cost, and independence.
          </p>
        </div>
        <a class="button compare-callout__btn" href="/compare/">See the comparison</a>
      </div>
    </div>
  </section>

  {/* TEMPORARY: comparison interstitial. Remove when the compare push ends. */}
  <dialog class="compare-modal" id="compare-modal" aria-labelledby="compare-modal-title">
    <button class="compare-modal__close" type="button" aria-label="Close">
      <Icon name="close" size={20} />
    </button>
    <h2 id="compare-modal-title" class="compare-modal__title">Choosing an integration engine?</h2>
    <p class="compare-modal__text">
      See how Open Integration Engine stacks up against BridgeLink and Mirth Connect on
      licensing, plugins, cost, and independence. An honest side-by-side.
    </p>
    <div class="compare-modal__actions">
      <a class="button compare-modal__go" href="/compare/">See the comparison</a>
      <button class="compare-modal__later" type="button">Maybe later</button>
    </div>
  </dialog>

  <section class="news">
    <div class="container">
      <header class="news__header">
        <h2>Latest news</h2>
        <a class="news__all" href="/news/">See all news →</a>
      </header>

      <ul class="news__grid">
        {latestNews.map((n) => (
          <li class="card card--hover card--lift news-card">
            <a class="news-card__link" href={n.href}>
              <time class="news-card__date" datetime={n.date.toISOString()}>{newsFmt.format(n.date)}</time>
              <h3 class="news-card__title">{n.title}</h3>
              {n.excerpt && <p class="news-card__excerpt">{n.excerpt}</p>}
            </a>
          </li>
        ))}
      </ul>
    </div>
  </section>

  <section class="plugins">
    <div class="container">
      <header class="plugins__header">
        <h2>Built by the community.</h2>
        <p>
          A growing ecosystem of open source plugins extends OIE into the work you actually
          need to do. Here are a few.
        </p>
        <a class="plugins__all" href="/plugins/">See all plugins →</a>
      </header>

      <ul class="plugins__grid">
        {plugins.slice(0, 4).map((p) => (
          <li class="card card--hover card--lift plugin-card">
            <a class="plugin-card__screen" href={p.repo} target="_blank" rel="noopener" aria-label={`${p.name} on GitHub`}>
              <img src={p.screenshot} alt={`${p.name} screenshot`} loading="lazy" style={p.imgPosition ? `object-position: ${p.imgPosition}` : undefined} />
            </a>
            <div class="plugin-card__body">
              <h3>{p.name}</h3>
              <p>{p.description}</p>
              <a class="plugin-card__link" href={p.repo} target="_blank" rel="noopener">
                View on GitHub
                <Icon name="arrow-up-right" size={13} />
              </a>
            </div>
          </li>
        ))}
      </ul>
    </div>
  </section>

  <section class="ecosystem">
    <div class="container">
      <header class="ecosystem__header">
        <h2>Backed by a real ecosystem.</h2>
        <p>Companies fund it, build on it, and deliver it to their customers.</p>
      </header>

      <div class="ecosystem__group">
        <div class="ecosystem__group-head">
          <h3>Sponsors</h3>
          <a class="ecosystem__all" href="/sponsors/">See all sponsors →</a>
        </div>
        <ul class="ecosystem__logos">
          {sponsors.map((s) => (
            <li>
              <a href={s.url} target="_blank" rel="noopener" title={s.name} aria-label={s.name}>
                <img src={s.logo} alt={s.name} loading="lazy" />
              </a>
            </li>
          ))}
        </ul>
      </div>

      <div class="ecosystem__group">
        <div class="ecosystem__group-head">
          <h3>Commercial support vendors</h3>
          <a class="ecosystem__all" href="/commercial-support/">See all vendors →</a>
        </div>
        <ul class="ecosystem__logos">
          {vendors.map((v) => (
            <li>
              <a href={v.url} target="_blank" rel="noopener" title={v.name} aria-label={v.name}>
                <img src={v.logo} alt={v.name} loading="lazy" />
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .hero {
    background: linear-gradient(0deg, var(--color-page-bg) 16%, #fed5d8 100%);
    padding: 4rem 0 3rem;
  }

  .hero__inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    align-items: center;
  }

  .hero__logo img {
    display: block;
    max-width: 420px;
    width: 100%;
    margin: 0 auto;
    border-radius: var(--radius-image);
  }

  .hero__lead {
    font-size: clamp(2rem, 3.6vw, 3rem);
    color: var(--color-black);
    font-weight: 700;
    line-height: 1.15;
    margin-bottom: 1rem;
  }
  .hero__lead span { display: block; }

  .hero__sub {
    font-size: 1.1rem;
    color: var(--color-base);
    line-height: 1.55;
    margin: 0 0 1.25rem;
    text-wrap: pretty;
  }

  .hero__cta {
    margin: 0.5rem 0 0;
  }

  .compare-callout {
    padding: 2rem 0 0.5rem;
  }
  .compare-callout__card {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.5rem;
    padding: 1.1rem 1.75rem;
  }
  .compare-callout__copy { text-align: left; }
  .compare-callout__title {
    font-size: 1.2rem;
    margin: 0 0 0.2rem;
  }
  .compare-callout__text {
    color: var(--color-base);
    margin: 0;
    font-size: 0.95rem;
  }
  .compare-callout__btn { flex-shrink: 0; white-space: nowrap; }
  @media (max-width: 640px) {
    .compare-callout__card { flex-direction: column; align-items: flex-start; }
  }

  /* TEMPORARY: comparison interstitial */
  .compare-modal {
    border: none;
    border-radius: var(--radius-card);
    padding: 2.5rem 2.25rem 2rem;
    max-width: 460px;
    width: calc(100% - 2rem);
    color: var(--color-base);
    box-shadow: 0 24px 70px rgba(24, 51, 67, 0.28);
    position: relative;
    text-align: center;
  }
  .compare-modal::backdrop {
    background: rgba(24, 51, 67, 0.55);
    backdrop-filter: blur(2px);
  }
  .compare-modal__close {
    position: absolute;
    top: 0.85rem;
    right: 0.85rem;
    width: 34px;
    height: 34px;
    border-radius: 50%;
    border: 0;
    background: transparent;
    color: var(--color-base);
    cursor: pointer;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    transition: background 0.15s ease;
  }
  .compare-modal__close:hover { background: var(--color-light-grey); color: var(--color-headings); }
  .compare-modal__title { font-size: 1.5rem; margin: 0 0 0.75rem; color: var(--color-headings); }
  .compare-modal__text { margin: 0 0 1.75rem; line-height: 1.6; }
  .compare-modal__actions {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    align-items: center;
  }
  .compare-modal__go { width: 100%; }
  .compare-modal__later {
    background: transparent;
    border: 0;
    color: var(--color-base);
    font-family: inherit;
    font-size: 0.9rem;
    font-weight: 600;
    cursor: pointer;
    padding: 0.25rem;
  }
  .compare-modal__later:hover { color: var(--color-headings); }

  .news {
    padding: 2rem 0 4rem;
  }
  .news__header {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 1rem;
    margin-bottom: 1.5rem;
  }
  .news__header h2 {
    font-size: clamp(1.75rem, 3vw, 2.25rem);
    margin: 0;
  }
  .news__all {
    font-size: 0.9rem;
    color: var(--color-accent);
    font-weight: 600;
    white-space: nowrap;
  }
  .news__grid {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
  }
  .news-card { display: flex; }
  .news-card__link {
    display: flex;
    flex-direction: column;
    padding: 1.5rem;
    height: 100%;
    color: inherit;
  }
  .news-card__link:hover { color: inherit; }
  .news-card__date {
    font-size: 0.8rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--color-base);
    margin-bottom: 0.5rem;
  }
  .news-card__title {
    font-size: 1.15rem;
    margin: 0 0 0.6rem;
  }
  .news-card__excerpt {
    color: var(--color-base);
    font-size: 0.93rem;
    line-height: 1.6;
    margin: 0;
  }

  .plugins {
    background: var(--color-surface);
    padding: 4rem 0 5rem;
    border-top: 1px solid var(--color-border-soft);
    border-bottom: 1px solid var(--color-border-soft);
  }

  .plugins__header {
    max-width: 640px;
    margin: 0 auto 2.5rem;
    text-align: center;
  }
  .plugins__header h2 {
    font-size: clamp(1.75rem, 3vw, 2.25rem);
    margin-bottom: 0.75rem;
  }
  .plugins__header p {
    color: var(--color-base);
    font-size: 1.05rem;
    margin: 0;
  }
  .plugins__all {
    display: inline-block;
    margin-top: 1rem;
    font-size: 0.9rem;
    font-weight: 600;
    color: var(--color-accent);
  }

  .plugins__grid {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
    max-width: 880px;
    margin-inline: auto;
  }

  .plugin-card { overflow: hidden; display: flex; flex-direction: column; }

  .plugin-card__screen {
    display: block;
    aspect-ratio: 16 / 10;
    background: var(--color-light-grey);
    overflow: hidden;
    border-bottom: 1px solid var(--color-border-soft);
  }
  .plugin-card__screen img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: top center;
  }

  .plugin-card__body {
    padding: 1.25rem 1.25rem 1.5rem;
    display: flex;
    flex-direction: column;
    flex: 1;
  }
  .plugin-card__body h3 {
    font-size: 1.15rem;
    margin: 0 0 0.5rem;
  }
  .plugin-card__body p {
    color: var(--color-base);
    font-size: 0.93rem;
    line-height: 1.55;
    margin: 0 0 1rem;
  }

  .plugin-card__link {
    margin-top: auto;
    align-self: flex-start;
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    color: var(--color-accent);
    font-weight: 600;
    font-size: 0.9rem;
    transition: gap 0.15s ease;
  }
  .plugin-card__link:hover { gap: 0.55rem; }

  .ecosystem { padding: 4rem 0 5rem; }

  .ecosystem__header {
    max-width: 640px;
    margin: 0 auto 3rem;
    text-align: center;
  }
  .ecosystem__header h2 {
    font-size: clamp(1.75rem, 3vw, 2.25rem);
    margin-bottom: 0.5rem;
  }
  .ecosystem__header p {
    color: var(--color-base);
    font-size: 1.05rem;
    margin: 0;
  }

  .ecosystem__group + .ecosystem__group { margin-top: 3rem; }

  .ecosystem__group-head {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 1rem;
    margin-bottom: 1.25rem;
    padding-bottom: 0.5rem;
    border-bottom: 1px solid var(--color-border);
  }

  .ecosystem__group-head h3 {
    font-size: 0.8rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--color-headings);
    margin: 0;
  }

  .ecosystem__all {
    font-size: 0.85rem;
    color: var(--color-accent);
    font-weight: 600;
  }

  .ecosystem__logos {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(140px, 1fr));
    gap: 1rem;
  }

  .ecosystem__logos li {
    aspect-ratio: 2 / 1;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 8px;
    padding: 0.75rem;
    transition: border-color 0.15s ease;
  }
  .ecosystem__logos li:hover { border-color: var(--color-border-strong); }

  .ecosystem__logos a {
    display: flex;
    width: 100%;
    height: 100%;
    align-items: center;
    justify-content: center;
  }
  .ecosystem__logos img {
    max-width: 100%;
    max-height: 50px;
    object-fit: contain;
  }

  @media (max-width: 900px) {
    .hero__inner {
      grid-template-columns: 1fr;
      text-align: center;
    }
    .hero__copy { text-align: left; }
    .news__grid { grid-template-columns: 1fr; }
    .plugins__grid { grid-template-columns: 1fr; }
    .ecosystem__logos { grid-template-columns: repeat(auto-fill, minmax(110px, 1fr)); }
  }

</style>

<!-- TEMPORARY: comparison interstitial -->
<script>
  const modal = document.getElementById('compare-modal') as HTMLDialogElement | null;
  if (modal && localStorage.getItem('oie-compare-modal') !== 'dismissed') {
    const dismiss = () => {
      localStorage.setItem('oie-compare-modal', 'dismissed');
      if (modal.open) modal.close();
    };
    setTimeout(() => modal.showModal(), 800);
    modal.querySelector('.compare-modal__close')?.addEventListener('click', dismiss);
    modal.querySelector('.compare-modal__later')?.addEventListener('click', dismiss);
    modal.querySelector('.compare-modal__go')?.addEventListener('click', dismiss);
    modal.addEventListener('cancel', dismiss);
    modal.addEventListener('click', (e) => {
      if (e.target === modal) dismiss();
    });
  }
</script>
